// The app's name, rendered as the page heading on both screens — the signed-out landing and the
// signed-in chrome — so signing in does not appear to switch the user to a differently named app.
export const APP_TITLE = "מעקב תזונה";
